<h1><%= @image.title %></h1>
<%= link_to 'Back', root_path %>
<br>
<%= image_tag @image.photo, alt: "Photo"%>

<%= link_to "See similar images", search_image_path(:id => @image.id, :similar => "true") %>

<% @image.tags.each do |tag| %>
  <li>
    <%= tag.name %>
  </li>
<% end %>
