<div>
  <h1>All Images</h1>
  <%= link_to 'New image', new_image_path %>
</div>

<ul>
  <% @images.each do |image| %>
    <li>
      <%= link_to image.title, image_path(image)%>
    </li>
  <% end %>
</ul>
